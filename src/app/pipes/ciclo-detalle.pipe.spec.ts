import { CicloDetallePipe } from './ciclo-detalle.pipe';

describe('CicloDetallePipe', () => {
  it('create an instance', () => {
    const pipe = new CicloDetallePipe();
    expect(pipe).toBeTruthy();
  });
});
