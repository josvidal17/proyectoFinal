import { AsigDetallePipe } from './asig-detalle.pipe';

describe('AsigDetallePipe', () => {
  it('create an instance', () => {
    const pipe = new AsigDetallePipe();
    expect(pipe).toBeTruthy();
  });
});
